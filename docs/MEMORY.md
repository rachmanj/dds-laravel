- Solar PERTAMINA solar price auto-sync (2026-04): `solar:price:sync-from-last-pertamina` + half-month period; `PertaminaSolarInvoiceResolver` derives `unit_price` from `amount ÷ quantity` when line unit price is null/0; schedule 07:30 Asia/Makassar. See `docs/SOLAR-PRICE-HISTORY-PERTAMINA-SYNC.md` and `docs/decisions.md` (2026-04-22).

- Invoice document import (v1, 2026-03): OpenRouter for PDF/image extraction; queue job `ExtractInvoiceFromDocumentJob`; `invoices.import_extraction` JSON on save when `import_uuid` used; temp file attached as Invoice Copy via `InvoiceImportAttachmentService`. Production needs `OPEN_ROUTER_API_KEY`, queue worker if `QUEUE_CONNECTION` is database/redis; `INVOICE_IMPORT_EXTRACT_SYNC=false` unless blocking extract in HTTP is acceptable. First-page-only for multi-page OCR PDFs when `OPEN_ROUTER_PDF_FIRST_PAGE_ONLY` true. See `docs/INVOICE-FROM-DOCUMENT-IMPLEMENTATION-PLAN.md` and `docs/architecture.md`.

- Implemented SAP ITO query sync on 2025-11-10: Uses Service Layer QueryService_ExecuteQuery for 'list_ito' with date params. Maps to AdditionalDocument mirroring ItoImport.php. Skips duplicates by ito_no only. Log to sap_logs for audit. Decision: Async job to handle large results without UI blocking; reuse import logic for consistency. (ID: 1234567)
- Implemented SAP A/P invoice creation on 2025-11-10: Async job with validation, payload mapping, retries, status updates, and logging. (ID: 7654321)
- Added reconciliation command on 2025-11-10: Hourly sync of SAP statuses to local invoices. (ID: 8765432)
